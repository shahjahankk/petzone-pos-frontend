# Standalone Queue API used by POS frontend (NOT the main POS backend)
NEXT_PUBLIC_QMS_API_URL=https://queue-management.petzone.pk/api

# Fallback branch slugs if POS branch is not linked yet
NEXT_PUBLIC_QMS_ORG_SLUG=petzone
NEXT_PUBLIC_QMS_BRANCH_SLUG=main

# LabelPress (same Node app = UI + API)
# Production:
NEXT_PUBLIC_BARCODE_APP_URL=https://barcode-printer.petzone.pk
# Local: http://localhost:5055
